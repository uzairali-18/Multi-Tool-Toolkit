plugins:
  - jekyll-feed

# Cache control for static assets
defaults:
  - scope:
      path: "**/*.+(html|css|js|jpg|png|svg|webp)"
    values:
      cache-control: "public, max-age=31536000, immutable"
